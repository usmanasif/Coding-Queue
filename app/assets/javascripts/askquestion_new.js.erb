$(document).ready(function () {
    $('textarea').autosize();

    $("button.create-new-question").live('click', function (e) {
        var current_user = $('#login_user_id').val();
        data = $("div#wmd-preview").html();
        $("input#wmd-preview-input").val(data);
        if (!current_user){
           if(!login_fancy_box())
           {
               return false;
           }

                    }


    });
});


